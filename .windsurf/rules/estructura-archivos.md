---
trigger: always_on
- Paginas en `app/` o `_pages/` (Next.js 15+ usar App Router).
- Componentes reutilizables en `components/`.
- Hooks en `hooks/`.
- Utilidades en `lib/`.
- Tipos globales en `types/`.
---
